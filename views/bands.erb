<% if @bands.any?() %>

  <h3>Here are the current bands:</h3>

  <ul>
    <% @bands.each do |band| %>
      <li><a href="bands/<%= band.id() %>"><%= band.name() %></a></li>
    <% end %>
  </ul>

<% else %>

  <h3>There are no bands yet!</h3>

<% end %>


<h3>Add a new Band</h3>

<form action="/bands" method="post">
  <div class="form-group">
    <label for="name">Name of Band:</label>
    <input type="text" name="name" id="name" class="text-center">
    <br></br>
    <button type="submit" class="btn btn-danger">Submit</button>
  </div>
</form>
